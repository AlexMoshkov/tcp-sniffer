handlers:
  - name: filter_name
    filter: ip
    save_capture:
      filepath: filepath
  - name: another_filter
    filter: port 23
    save_capture:
      filepath: ppp


# mb user can write own c handler and connect it through config
# mb user can write template string to save_capture